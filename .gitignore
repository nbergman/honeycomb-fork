# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.local
.env*.local

# parcel-bundler cache (https://parceljs.org/)
.cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Old Repositories
hivemind-v1/
otherhive-v1/

# Honeycomb build outputs (PRD-001a)
/dist/
**/bundle/
/jscpd-report/

# Honeycomb esbuild per-target bundle outputs (PRD-001b)
# Generated by `node esbuild.config.mjs`; shipped via the `files` allowlist on
# prepack, never committed to source.
/daemon/
/sdk/
/harnesses/openclaw/dist/
/embeddings/embed-daemon.js
/embeddings/chunks/
/harnesses/openclaw/dist/chunks/

# get.theapiary.sh Cloudflare Pages publish dir (PRD-050a install-hosting follow-up).
# Generated by `node site/install/build.mjs` from the canonical scripts/install/ sources;
# the scripts have a SINGLE source of truth — never commit the generated copies.
/site/install/dist/

.cursor/
.claude/

# Stryker mutation-testing artifacts (npm run mutation): per-run sandboxes and
# the JSON/HTML reports. The stryker/ configs ARE committed; their outputs are not.
.stryker-tmp*/
/reports/mutation/
stryker.log
# PRD-021: `src/daemon/runtime/logs/` is a REAL source module (the daemon's /api/logs
# handler), and its mirror test dir — NOT runtime log output. The bare `logs` rule above
# (Node template) is too broad and silently swallowed them. Un-ignore these source paths.
!src/daemon/runtime/logs/
!tests/daemon/runtime/logs/

# Daemon runtime state — machine-local, never commit
.secrets/
.vault/
.settings/
.daemon/
.smoke-daemon-bundle/

# PRD-034a: the live-integration suite's run-level INFRA-DEGRADED sentinel marker
# (tests/integration/_infra-skip.ts). Pure run-metadata the Wave-2 ci.yaml job reads
# to map a sustained backend outage to a NEUTRAL conclusion — never committed.
.infra-skip/

# PRD-034b: the on-demand DeepLake stress harness's metrics report output
# (scripts/deeplake-stress.mjs → ./.stress-report/<runId>.json). A local/CI
# diagnostic artifact (uploaded by the Wave-2 workflow_dispatch job) — never committed.
.stress-report/

# Security-worker-bee close-out scratch dir (grep findings, npm-audit json, scan
# summaries). A local audit artifact written during the security pass — never committed.
.scan-output/

.DS_Store
